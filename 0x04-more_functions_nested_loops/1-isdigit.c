#include "main.h"
/**
 *_isdigit - checks uppercase character
 *@c: variable c
 *
 *Return: one if c is a digit, zero if otherwise
*/

int _isdigit(int c)
{

	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
