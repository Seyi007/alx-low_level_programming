#include "main.h"
/**
 *_isupper - checks uppercase character
 *@c: variable c
 *
 *Return: one if c is uppercase, zero if otherwise
*/
int _isupper(int c)
{
	int c;

	if (c == 'C')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
