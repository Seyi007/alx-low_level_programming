#include "main.h"

/**
 * _isalpha - checks for lowercase
 * @c: c is an ascii character
 *
 * Return: 1 if lowercase
 */

int _isalpha(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else if (C >= 'A' && C <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
