#include "main.h"
#include <stdio.h>
/**
 *_strlen - prints the lenght of a string
 *@s: variable name for string
 *
 *Return: 0 always
 */

int _strlen(char *s)
{
	int len;

	len = strlen((int)*s);
	return (len);

}
